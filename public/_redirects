# Sponsor portal routing is handled by functions/sponsor/[[path]].js.
# This file only handles /checkin/{token} since checkin runs through
# middleware (cookie auth check) and a function isn't desired there.
/checkin/*  /checkin/index.html  200
